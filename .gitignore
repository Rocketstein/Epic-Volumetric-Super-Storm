# Unreal generated / build output — matches these dir names at any depth
Binaries/
Build/
DerivedDataCache/
Intermediate/
Saved/

# IDE / editor caches
.vs/
.vscode/
.idea/
*.VC.db
*.VC.opendb
*.suo
*.sdf
*.opensdf

# Generated solution / project files
*.sln
*.slnx
*.xcodeproj
*.xcworkspace
*.DotSettings.user

# OS cruft
.DS_Store
Thumbs.db
desktop.ini

# Claude Code local metadata (already p4-ignored)
.claude/

# Binary content — source-only checkpoint. Perforce owns binaries.
# Delete this line (and set up Git LFS) if you want git to version Content too.
Content/
/Docs/StormFlowMapLayerWeightPlan.md
